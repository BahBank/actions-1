name: My First workflows

on:push
jobs:
    first_job:
        runs-on: ubuntu:latest
        steps:
        - name: Welcome message
          run: echo "My first github Action"

        - name: liste files
          run: ls
        
        - name: Read file
          run: cat README.md